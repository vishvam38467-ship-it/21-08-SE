# pro1: Use list comprehension to generate all even numbers between 10 and 50 inclusive
even_numbers = [num for num in range(10, 51) if num % 2 == 0]
print("pro1 - Even numbers 10 to 50:", even_numbers)
